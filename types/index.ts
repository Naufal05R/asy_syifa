export interface ItemProps {
  index: number;
  className?: string;
  item?: object;
}

export interface supportsProps {
  category: string;
  performer: string;
  description: string;
}

export interface StatsProps {
  banner: string;
  text: string;
  subText: string;
}
